#include<stdio.h>

int main(){
  int testCase;
  int n,p;
  int cake,hard;
  int temp;

  scanf("%d", &testCase);
  while( testCase-- ){
    cake = hard = 0;
    scanf("%d %d", &n, &p);
    while(n--){
      scanf("%d", temp);
      if(  ){

      }
    }
  }
  return 0;
}
